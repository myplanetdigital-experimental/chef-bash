# Chef assumes that it owns this file.
# Additions to bash should be placed into <%= @bash_profile_subdir %> with a .sh extension

for file in ~/<%= BASH_PROFILE_DIRNAME %>/*.sh; do
  [[ -r $file ]] && source $file;
done
